import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.christolake.yugioh',
  appName: 'yugioh-card-colllector',
  webDir: 'dist'
};

export default config;
