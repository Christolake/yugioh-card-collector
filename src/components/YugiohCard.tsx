import type { CardProps } from "../types/CardProps";

const YugiohCard : React.FC<CardProps> = ({
    id, frameType, name, attribute, level, imageUrl,
    setCode,desc, race, typeline, atk, def, linkval,
    linkmarkers, scale, edition
}) => {

}